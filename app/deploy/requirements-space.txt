# Lean CPU runtime deps for the free Hugging Face Docker Space.
# Uses the CPU-only PyTorch wheels (no CUDA) to keep the image small.
# Kraken/DocLayout-YOLO are intentionally omitted for a reliable free build —
# the pipeline falls back to the projection segmenter. Add them back (and a GPU)
# for production-grade segmentation.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0
torchvision==0.27.0
transformers==5.10.2
accelerate==1.13.0
sentencepiece==0.2.1
protobuf==7.35.0
Pillow==11.3.0
numpy==2.4.4
pandas==3.0.3
sentence-transformers==5.6.0
faiss-cpu==1.14.3
fastapi==0.138.2
uvicorn[standard]==0.49.0
python-multipart==0.0.32
requests==2.34.2
