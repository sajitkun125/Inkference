# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
.python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# PEP 582; used by e.g. github.com/David-OConnor/pyflow
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
# Backups and per-machine variants of .env hold the same secrets the file does, and
# the bare `.env` rule above does not cover them. `.env.example` is the one exception:
# it is the checked-in template and holds no values.
.env.*
!.env.example
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/


# Mac Stuff
.DS_Store

# vscode Stuff
.vscode

# Data 
data/

#Checkpoints
models/bentham_trocr_checkpoint/

#Large and not necessary notebooks
visualizeFinalTranscriptionLinesAndGT.ipynb

# Inkference platform: runtime data + assembled inference model + build artifacts
.inkference_data/
models/
src/*.egg-info/
*.egg-info/

# Agent conversation memory. Holds users' questions verbatim and is per-machine —
# never commit it, and note that deploy_all_books.sh copies only inkference.db +
# index/ into the public seed dataset, so it can't ship there either.
agent_checkpoints.db
agent_checkpoints.db-shm
agent_checkpoints.db-wal

# SQLite sidecars (WAL mode) — transient, and they make the tracked .db look dirty.
*.db-shm
*.db-wal

# Local dev server logs
app/server*.log
.pytest_cache/

# Internal build/design notes — kept locally, not published with the repo.
projectNotes/

